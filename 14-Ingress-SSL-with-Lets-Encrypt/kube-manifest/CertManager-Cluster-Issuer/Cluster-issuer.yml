---
apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt
spec:
  acme:
    - server: https://acme-v02.api.letsencrypt.org/directory
      email: mailmeritesh.99@gmail.com
      profile: tlsserver
      privateKeySecretRef:
        name: letsencrypt-staging
      solvers:
        - http1:
            ingress:
              class: nginx